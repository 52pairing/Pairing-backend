spring:
  # 이 파일은 src/main/resources/application.yaml 을 **통째로 가린다**(같은 이름이라 하나만 읽힌다).
  # AI 매칭의 재시도·차단기 설정을 여기서도 불러오지 않으면 테스트는 resilience4j 기본값으로 돌고,
  # 정책 테스트가 "실제와 다른 설정"을 검증하게 된다 - 실제로 502 재시도 여부가 뒤집혔다.
  config:
    import: classpath:matching-resilience.yaml

  datasource:
    driver-class-name: org.h2.Driver
    # 운영은 PostgreSQL이므로 H2도 PostgreSQL 호환 모드로 둔다.
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

spring.mail:
  host: localhost
  port: 3025

app.crypto:
  # 테스트 전용 더미 키(32바이트 Base64). 운영 값과 무관하다.
  data-key: dGVzdC1kYXRhLWVuY3J5cHRpb24ta2V5LTMyYnl0ZXM=

jwt:
  secret-key: test-secret-key-for-unit-test-please-change-me-32bytes+
  access-token-expiration: 1800000
  refresh-token-expiration: 604800000
  cookie-domain: ""
  cookie-secure: false

app:
  cors:
    allowed-origins: http://localhost:17000
  websocket:
    endpoint: /ws

ai:
  pairing-python:
    base-url: http://localhost:8000
    internal-api-key: test-internal-api-key

# Lettuce는 지연 연결이므로 실제 Redis 없이도 컨텍스트는 기동된다.
# Redis를 실제로 쓰는 테스트는 로컬 Redis(또는 Testcontainers)를 띄워야 한다.
spring.data.redis:
  host: localhost
  port: 6379

cloud:
  aws:
    s3:
      bucket: test-bucket
    credentials:
      access-key: test
      secret-key: test
    region:
      static: ap-northeast-2
