# AGENTS.md

이 프로젝트에서 AI가 작업할 때 따르는 기본 가이드입니다.

코드를 바꾸기 전에 요청을 분류하고, **그 작업에 필요한 문서만** 읽으십시오. 매번 전체 문서를 읽지 않습니다.

## 핵심 규칙

- 기존 아키텍처, 패키지 구조, 네이밍, 코드 스타일을 유지합니다.
- 작업을 마치는 데 필요하지 않다면 요청 범위 밖의 파일을 수정하지 않습니다.
- 기존 파일에 광범위한 포매팅, import 정렬, 공백만 바꾸는 변경을 하지 않습니다.
- 정책이 불명확하고 선택에 따라 결과가 크게 달라지면 먼저 물어봅니다.
- 코드 변경 후에는 가능한 범위에서 검증 명령을 실행합니다.
- 시크릿, 토큰, 비밀번호, 개인키, 실제 인증정보 값을 코드나 문서에 쓰지 않습니다.
- 코드와 주석에 이모지를 쓰지 않습니다.

## 요청 라우팅

### 백엔드 작업

Java, Spring Boot, JPA, Controller, Service, Repository, Entity, Mapper, ErrorCode, Swagger, validation, 트랜잭션, 헥사고날 구조 관련 작업:

- `docs/ai/backend-convention.md`를 읽습니다.
- API 계약이 바뀌면 `.ai/API.md`도 읽고 갱신합니다.
- 검증이 필요하면 `docs/ai/testing-guide.md`를 읽습니다.

### API 계약 작업

엔드포인트, 요청/응답, 상태코드, 에러코드, 인증/인가, 프론트엔드 연동이 바뀌는 작업:

- `.ai/API.md`와 `docs/ai/backend-convention.md`를 읽습니다.
- 변경 후 `.ai/API.md`를 갱신합니다.

### 파일 업로드 / Redis / 보안 작업

인증, 인가, 토큰, 환경변수, 파일 업로드, 개인정보, 시크릿 처리가 바뀌는 작업:

- `docs/ai/security-guide.md`를 읽습니다.
- 스토리지·캐시 사용법은 `docs/ai/infrastructure-guide.md`를 읽습니다.

### 기능 요구사항 / 정책 확인

무엇을 만들어야 하는지, 어떤 기준값(수수료율, 기한, 횟수 상한 등)을 써야 하는지 판단이 필요한 작업:

- `docs/spec/README.md`를 읽고, 필요한 업무구분만 `docs/spec/requirements.md`와 `docs/spec/policy.md`에서 확인합니다.
- 요구사항과 정책이 상충하면 정책을 기준으로 하고, 상충 사실을 보고합니다.
- 정책이 미확정(`정의 필요`)인 항목에 의존하는 로직은 임의로 값을 정하지 않고 먼저 물어봅니다.

### AI 서버(FastAPI) 작업

임베딩, 매칭 추천, LLM 호출, 파이썬 코드 관련 작업:

- `ai-server/README.md`를 읽습니다. 구조·통신 규약·DB 소유권 규칙이 거기 있습니다.
- 스프링과 주고받는 계약(헤더, 응답 형식, 에러코드)이 바뀌면 양쪽 문서를 함께 갱신합니다.

### 이전 작업 이어서

"이어서", "어디까지 했어", "현재 상태 알려줘" 같은 요청:

- `.ai/STATE.md`와 `.ai/HANDOFF.md`를 읽습니다.
- 계속하기 전에 현재 상태를 먼저 요약합니다.

### 완료 / 인수인계

"완료", "끝났어", "기록해줘", "정리해줘" 같은 요청:

- `.ai/WORKLOG.md`를 갱신합니다.
- 진행 중인 작업이 바뀌었으면 `.ai/STATE.md`를 갱신합니다.
- 다른 사람이 이어받을 수 있으면 `.ai/HANDOFF.md`를 갱신합니다.
- API가 바뀌었으면 `.ai/API.md`를 갱신합니다.

### 테스트 / 빌드 작업

테스트 실패, 빌드 실패, 검증 요청:

- `docs/ai/testing-guide.md`를 읽습니다.
- 코드 수정이 필요할 때만 백엔드 컨벤션을 추가로 읽습니다.

### Git / 이슈 / 커밋 / PR 작업

브랜치, 이슈, 커밋 메시지, PR 제목·본문, 리뷰 준비:

- `docs/ai/git-issue-pr-guide.md`를 읽습니다.
- 사용자가 명시적으로 요청하지 않으면 commit, push, PR 생성을 하지 않습니다.

## 작업 완료 체크리스트

코드 작업을 끝내기 전에:

1. 변경된 파일이 요청 범위와 일치하는지 확인합니다.
2. 패키지 의존 방향과 아키텍처 규칙이 여전히 유효한지 확인합니다.
3. 가능하면 관련 테스트/빌드 명령을 실행합니다.
4. 무엇을 바꿨고 어떤 검증을 했는지 보고합니다.
5. 실행하지 못한 검증이 있으면 그것도 명시합니다.
