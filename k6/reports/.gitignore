# 실행 결과는 커밋하지 않는다. 매 실행마다 레포가 더러워지고, 리포트는 실행한 사람의
# 로컬 환경(네트워크 지연 포함)에 묶인 값이라 남겨 둘 의미가 적다.
#
# 이 .gitignore 자체는 커밋한다 — 그래야 폴더가 존재하고, k6 가 리포트를 쓸 수 있다.
# (k6 는 없는 폴더를 만들지 않는다. 폴더가 없으면 실행 끝에 저장이 실패한다)
*
!.gitignore
